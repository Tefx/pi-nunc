# Bounded stock-host observations

`scripts/verify-live.mjs` consumes one nonsecret JSON object on stdin and runs the locked stock Pi CLI/RPC or actual PTY/TUI. It supplies verification tools, without replacing the product launcher or constructing an SDK host. Node 26.7.0 and Pi/pi-ai 0.85.1 are the selected targets.

**Stable-memory observation:** [STABLE-MEMORY.md §11](STABLE-MEMORY.md#11-验收与模型测试边界) scenarios `m1`–`m4` are tracked. `m1` compares moving vs fixed layout (`moving` is observer-only, not a production fallback). `m2` is M-only update/correction/deletion task use without prompt-coached answers. `m3` composes the live observer, Nunc and named payload-append. `m4` compares explicit `keepRecentFraction` `0.67` and `0.5` under fixed layout and rejects `retentionCalibration`. Public template: `examples/stable-memory-selection.json`. Material predicates live in `src/live/stable-memory-observation.ts`. Gemini first, bounded GPT Luna only where needed, **no Astra main, maintenance or model-observer calls and no fallback to Astra**. Verify actual native selection before calls. Historical Astra evidence and controlled offline fixtures are not new live calls. Explicitly load the candidate Nunc into isolated task state without re-enabling it in daily settings.

## Defaults and ownership

Required input is `target`, `limits`, and `scenarios`. Pi resolves its effective native model/provider/options/configuration; callers do not reconstruct a model catalog or repeat approval text. Existing full-delivery authorization applies. Only named test requirements justify `overrides`, with their reason and actual differences recorded.

Standalone invocation resolves Pi's startup defaults. To preserve an invoking runtime's unsaved current choice, forward its nonsecret effective selection from public Pi context/invocation facilities. Another process cannot implicitly discover that selection. Static settings must never override the forwarded choice. Default main output/thinking remains unchanged; capacity failure requires a diagnostic or a named test override.

New task sessions, cwd/files, artifacts and bounded effects are isolated. The existing native configuration/authentication owner stays in place. Nunc does not copy a profile or credential record, export tokens, manage OAuth storage/refresh/login, or require an API-key account. There is no auth-handoff command or authentication preparation phase. Pi performs ordinary credential lookup/refresh/persistence; native credential bytes are not promised frozen. Existing daily sessions and saved settings/default selections remain protected. Credential values and source locations are excluded from recorded stdin, argv and reports.

Producer tests use fictional pre-existing native profiles and controlled services only. Real native authentication and model observations belong to the downstream runtime owner. Controlled transport/compaction fixtures prove mechanics, with no claim about memory quality or live authentication.

### Native startup model

The native RPC child is launched with the authorized `--provider`/`--model`. Loaded extensions may still change the session model during initialization. Actual Larva applies `larva.defaultPersona` from inherited `HOME` / `PI_CODING_AGENT_DIR` settings when it does not treat the CLI selection as `cliSelectedModel`; `--larva-agent-persona-switch manual` does not skip that path. After the stock session binds extensions, NativeHost reads current `get_state` and, if that model is outside `modelTargets`, restores the authorized CLI selection through public `set_model`. Admission still requires the current `get_state` model. Observer `ready` is not current-state authority. Later unauthorized `set_model` still fails. This does not fake `LARVA_PI_LAUNCHED`, remove the prompt bridge, rewrite global persona registration, or change general's own model. Isolated `compactionOwner: "nunc"` remains a task-local single-owner setting; default dual-hook composition stays unsupported.

## Invocation and bounds

```sh
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/verify-live.mjs --preflight < selection.json
# Execute the same selection after successful no-effect preflight:
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/verify-live.mjs < selection.json
```

`--preflight` resolves nonsecret configuration, checks the locked build and actual target/limits, and makes no model call, auth resolution/copy, new task session or installation. `--observe-stock` runs named controlled RPC/TUI checks. Missing prerequisites fail explicitly; RPC cannot substitute for TUI. `PI_OFFLINE` suppresses startup/catalog refresh, without limiting model calls itself.

The exact public schema is:

- `target: {repository, stateRoot, cleanup: "retain" | "remove"}` with canonical absolute paths.
- `limits: {maxCalls, maxTotalTokens, maxDurationMs, maxOutputTokens, maxCostUsd}`. `maxCalls` and `maxTotalTokens` may be omitted or `null`; the runner normalizes each to `number | null`. `null` disables that cumulative gate: there is no finite fallback, `Infinity` sentinel, or reserved-token substitute for a call count. An explicit positive safe integer remains opt-in and is enforced before the corresponding effect, including across comparison groups, segments and stock subjobs. There is no agent `maxCalls <= 1000` ceiling. `maxDurationMs` and `maxOutputTokens` stay positive finite integers. `maxCostUsd` is positive for a known-cost reservation or null for unknown billing. Codex defaults an omitted cost ceiling to null. Omitted `maxOutputTokens` resolves to the selected native output allowance; it never lowers Pi's main output. Per-request native input/output/context capacity, independent time/cost, cancellation, unknown-terminal refusal, endpoint/auth/identity and cleanup stay in force even when both totals are omitted.
- `scenarios: [{id: "c1" | "c2" | "c3" | "c4" | "c5" | "g1" | "g2" | "g3" | "g4" | "g5" | "g6" | "g7" | "g8" | "m1" | "m2" | "m3" | "m4", variant?: "full" | "capacity" | "late-d" | "conflict" | "unconfirmed" | "fits-required" | "required-too-large" | "moving" | "fixed" | "keep-0.67" | "keep-0.5"}]`; c4 requires `full` or `capacity`. c1 may select `late-d`. g7 may select `conflict` or `unconfirmed`. g8 requires `fits-required` or `required-too-large`. m1 requires `moving` or `fixed`. m4 requires `keep-0.67` or `keep-0.5`. Other cases have no variant.
- Optional `overrides: [{requirement, reason, model?: {provider,id}, smallerModel?: {provider,id}, thinking?, config?: {nunc?, compaction?, retentionCalibration?}, scenario?: "id[/variant]", extension?: "/absolute/larva.ts", compactionOwner?: "nunc"}]`. Partial compaction overrides merge with native defaults. Calibration supplies `minFraction`/`maxFraction`; c5 requires a named smaller-model selection. A `scenario` override supplies only `config`, uniquely names a selected scenario, and applies after shared overrides; its `nunc` object replaces the shared `nunc` object. Model/thinking routing remains shared. Reports include resolved per-scenario before/after configurations.
- Optional `observations`: `continuation` alone (default), or `stock_rpc`/`stock_tui` for controlled-only observations.

`examples/live-selection.json` is a path template using native defaults. `examples/guidance-selection.json` provides a template for working-memory guidance scenarios (`g1`–`g8`). `examples/stable-memory-selection.json` selects `m1`–`m4` with explicit Gemini routing and explicit 0.67/0.5 fractions. Named live-route overrides bind catalog ids without probing accounts: `openai-codex`/`gpt-5.6-luna`, `openrouter`/`google/gemini-3.8-flash`, and `xai`/`grok-4.6` for Grok 500k reproduction, with `thinking: "low"` when the model advertises that level. For guidance scenarios, OpenRouter `google/gemini-3.8-flash` is the authorized live model; Astra and non-Gemini models are rejected. Stable-memory live observations forbid Astra; they do not change other scenario model rules. No `authorization`, `models`, `credentials`, `receipt`, credential path or preparation fields are accepted on public stdin. Internal synthetic job fixtures under `tests/live` include normalized metadata solely to test private quota/worker interfaces; they are not operator templates.

Public `ModelRuntime.create` with an empty in-memory credential store and `refreshOnCreate:false` resolves nonsecret model configuration, including native models.json endpoint/capacity overrides, without opening native auth or constructing a session. The runner carries that native-resolved metadata; it does not reconstruct a provider-name or API support catalog or fall back to a hardcoded factory. Unsupported model headers/sampling overrides or endpoints containing credentials/query data fail before effects.

`PI_PROVIDER`, `PI_MODEL`, and `PI_REASONING_LEVEL` are Pi's public shell-tool forwarding path. Without those values, startup resolution uses saved settings and persisted/default project trust. No-call preflight refuses an unresolved startup model rather than probing accounts to choose one. Other running extensions' unsaved configuration and transient project-trust choices are not discoverable from another process; use a supported standalone invocation or explicitly forward the required nonsecret differences. Nunc itself always receives the active host's public context and actual request options.

The live observer extension registers `before_provider_request` observe-only then identity-return handlers so native runs compose callbacks. This hook records field names only. For m* observations, the bounded provider separately records the final serialized payload after all public payload hooks, under its explicit call ID; these task-local request bodies are observation artifacts and must be handled like the task history. No auth headers are recorded. Named override `payload-append` (with a test reason) inserts `-e dist/src/live/append.js` after the observer. That tracked extension appends the fixed synthetic last-user string `nunc-synthetic-last-user-append` through the public callback; it does not read ambient sensors, stdin text, or personal context. `payload-append-b` loads `dist/src/live/append-b.js` with a second distinct synthetic suffix so two append hooks compose. `provider-wrap` loads `dist/src/live/wrap.js`, which wraps the current native Provider once on `agent_settled` so the next turn re-captures the chain. Admission reports `transform: last-user-text-append` without the appended text. Maintenance header/auth proof uses public Provider/auth configuration; SDK-only header hooks are not claimed on `registry.complete`. The verification-only `bounded-observation` requirement records its restricted tools/context/resources, SSE and zero-retry differences. It preserves main output/thinking. The new scenario cwd receives only a narrow nonsecret task settings overlay (compaction and request/delivery options) through Pi's native project settings, with invocation-only `--approve`; no saved trust decision or existing repository/daily `.pi` directory is modified. Native global model configuration and credential ownership stay inherited. No whole profile is cloned.

`target` identifies the checkout and a new canonical task root beneath its `.scratch/` or the system temporary directory, plus retain/remove cleanup. `limits` always bounds time, total output and known costs. Cumulative call count and reserved-token totals are optional. Main and maintenance share one ledger; they consume a call/token cap only when that cap is configured. Preserve single-use target binding and uncertain-effect reconciliation; do not rerun an executed target. No separate default-inheritance receipt or free-form approval is required.

Each reservation still records `model.contextWindow + authorized output` as `reservedTokens` with a unique call id, actual terminal/usage, aggregate counts and known/unknown cost, including when `maxTotalTokens` is null. When `maxTotalTokens` is a finite cap, that charge is compared against remaining authorization. The charge is not a claim that input and catalog output occupy the window at once: product admission owns input headroom, native serializers clamp wire output, and the observer refuses only when the input estimate itself cannot leave the serializer floor. Grok-class 500k/500k main requests therefore reserve 1,000,000 tokens per call without being rejected before native clamp. Uncapped native Codex requests reserve `model.maxTokens`, without inventing `max_output_tokens`. The native provider owns serialization, compression, authentication and transport. `maxCostUsd: null` is user-authorized absence of a USD ceiling, not a free invoice; catalog estimates and native zero placeholders remain separately labeled and do not prove billing. Known costs enforce their configured bound. Reservations are never refunded. A completed failed independent scenario is not retried or replayed, but later authorized isolated scenarios may continue within remaining shared time/known-cost limits and any configured call/token caps. Unresolved requests, user cancellation, exhausted configured limits, and binding or cleanup failures still stop further effects. Local cancellation cannot prove remote cancellation or final billing. Native provider failures report a small sanitized diagnostic (stage, fixed code, observed HTTP status, whether transport started) on the consumed terminal and report; they do not copy bodies, headers, URLs, stderr or credentials, and they do not invent a private log to inspect.

The orchestration worker runs in the checked repository cwd and retains the supervisor's `TMPDIR` (including its absence), so both validate targets against the same allowed temporary root. The native task host alone redirects temporary files into `<stateRoot>/tmp`; no additional filesystem roots are allowed.

Worker boundary rejections appear in `children[].diagnostic` as a bounded code and fixed nonsecret message, and the supervisor's `reason` preserves that code. The separate IPC diagnostic does not capture stdout/stderr, exception bodies, environment, authentication or model responses. A terminated worker without a structured rejection still has its exit/signal/timeout receipt. Retained completed runs write `owner.json.status: "terminal"` with the report result and cleanup disposition after writing the terminal report. This marks local orchestration completion only; unresolved calls remain unresolved and the target remains single-use.

The supervisor propagates signals/deadline cancellation, waits for child exit, and escalates termination after a bounded grace. Failures retain task state and reports for reconciliation. Successful `remove` cleanup follows exited children and reconciled calls, preserving evidence in the returned report first. Native configuration/authentication outside task state is never removed.

## Stable-memory observation procedure
### Single compaction-owner prerequisite

The supplied actual Larva extension enables its own native-compaction hook by default. When loaded after Nunc, Pi's last nonempty `session_before_compact` result can replace Nunc's summary and cut. This default dual-compaction combination remains unsupported: the retained default-hook failure is real, and passing the separate eight-case Larva suite does not refute it. Reversing load order would still permit duplicate maintenance calls.

For explicit stable-memory Nunc/Larva observations, select `compactionOwner: "nunc"` on the `stable-memory-larva` override. The runner writes `<caseRoot>/larva-compaction.json` containing `{"enabled":false}` and selects it only in that child through Larva's public `LARVA_PI_COMPACTION_CONFIG_FILE` seam. It retains the actual Larva extension, manual persona mode and prompt resolver, while Nunc alone supplies compaction. The report records the child's selected task-local configuration; every observed Nunc candidate must match the persisted summary, cut and memory. No daily/global Larva configuration or production default changes. Omitting this explicit owner does not silently disable Larva's competing hook.

The parent independently approved this isolated single-owner setup on 2026-09-13 as preserving the accepted prompt-resolution/native-ownership contract. The observation therefore covers this configured composition, without claiming universal default-Larva compatibility.

### Workload and evidence

Use `examples/stable-memory-selection.json`; replace `__CHECKOUT__`, `__NEW_CANONICAL_TASK_ROOT__`, and `__ABSOLUTE_LARVA_EXTENSION__` with the committed target, a fresh canonical task root, and the approved read-only Larva source. Run the two `verify-live.mjs` commands above on the same selection. The template stays within the accepted aggregate ceilings of 160 calls and 200,000,000 reserved tokens; these bounds include main and maintenance. They do not guarantee that a real model completes every scenario. Inspect the native metadata and remaining authorization before execution; an exhausted or incomplete run remains unproven and cannot be silently retried with a new allowance.

The named `stable-memory-larva` override accepts `extension: "/absolute/larva.ts"` and explicit `compactionOwner: "nunc"`. Preflight binds that explicit source into the execution receipt. Hosts load it read-only with `--larva-agent-persona-switch manual`; the runner neither chooses a persona nor dispatches model observers/children. Active-persona borrow/restoration mechanics are separately exercised by `tests/pi/larva.integration.ts`. The example also loads `provider-wrap`, which exercises native provider recapture without changing M's text. `payload-append` remains available for deliberate payload-change observations; appending to M correctly prevents a positive exact-delivered-content result.

| Scenario | Workload and qualification | Evidence to consume |
|---|---|---|
| m1 fixed/moving | Read a bounded generated scratch file in native pages, retain the project identifier, append ordinary tasks, update the retry constraint, then continue. At least three same-content requests in each of two actual content epochs must have observed input + cache of at least 17,000 tokens; each new epoch starts at the request tail. | `layouts`, request `callId`, matching ledger terminal/cache fields, actual memory content/index, final payload and task artifacts. The 17,000-token floor qualifies scale; it does not establish a provider cache threshold or promise hits. Missing/short epochs remain UNPROVEN. |
| m2 | Read the original code, remove its file, retire its reading turn, and use M to write `initial.json`. Repeat for a correction. Delete M, retire the deletion turn's memory-read/tool results, and request a final artifact. | Three pre-turn task-file/current-R/M scans plus three first-delivered-Context checks establish M-only availability. The controller moves `initial.json` and `corrected.json` to observer-only `observed-artifacts/` after recording them, so they cannot supply later answers. `artifactSnapshots`, turn-complete memory states and `unlock.json` preserve actual actions for independent semantic judgment. No session entry is deleted. |
| m3 | Native file and memory tools execute with Nunc, the observer, provider recapture and explicitly supplied Larva. | Actual request/admission/final payload and task artifact. Loaded composition alone supplies no task-quality verdict. |
| m4 keep-0.67/keep-0.5 | Ten identical-sized scratch-reading batches under each explicit q, with a continuing policy needed afterward. The opt-in `stable-memory-bounded-window` derives reserveTokens once from native metadata to set the same 24,000-token native threshold in both variants. No manual compaction or q calibration is performed. | `rolloverSeries` records every actual checkpoint/cut/keep target, before/continuation call IDs, observed input before/after, released input difference and counts of main requests between thresholds. Require at least three threshold rollovers for two intervals. `segmentUsage` covers main plus maintenance; null cost remains unknown. Artifacts and tool actions support omission/reread/rework judgment. |

Only m2 uses its explicitly authorized retirement calibration, solely to put source turns outside current R. This control is independent of the m4 retention comparison. A real model may reread, fail to retain a code, restore deleted information, or produce a different artifact; raw observations remain available and failed qualifications are never promoted to positive evidence.

`layouts` uses consumed admission provenance and detached expected content. It distinguishes explicit empty M from absent association; timestamp-zero, timestampless and identical-envelope user text never establishes provenance. No index alone proves unique M. All required rows participate in each check. The bounded provider captures final serialized payloads after public hooks with an explicit call ID; completed usage is joined to that same call. The automated final-payload predicate currently supports textual OpenAI chat-completions, comparing ordered user correspondence and complete tool-call/result records. Genuine tool text `No result provided` is accepted when it came from the actual Context. Missing payloads, unsupported modalities/native formats, or missing rows remain UNPROVEN; changed records contradict preservation without assigning their cause to a serializer from a substring. Other native formats retain raw payloads for scoped independent inspection.

`tests/live/stable-memory-native.test.ts` executes all six variants through the public runner and asserts exit 0, completed turns, nonempty qualified layouts, source retirement, real automatic intervals, artifacts and ledger linkage. Its loopback responses test mechanics only. Its semantic artifact checks deliberately remain UNPROVEN. Supplying `NUNC_LARVA_EXTENSION` additionally exercises the same runner with the actual read-only extension. Preserve its printed `.scratch/stock/run-*/` directory, native sessions, ledger, final payloads and report until the acceptance consumer releases them; failed runs are retained too. This test creates no actual model calls.
## Continuation and evidence

Each scenario uses a fresh persistent session and task cwd. Public tools are bounded to scenario files. `read` of the task cwd itself is allowed so the native file tool can return its ordinary directory error; write/edit of that root, traversal, protected names and external symlinks stay denied. Observer denials distinguish path, deadline, tool-kind and write-size without collapsing them into one outside-scope sentence. Before stock bash execution, the observer requires a provided fixture `verify.py`, a task-local regular file, and its original bytes. An absent/unprovided/missing/modified script is refused even when the command name matches. Verification is also refused in a batch containing a sibling write/edit of that script: Pi preflights siblings before their concurrent execution. Blocked attempts are recorded separately from executed calls; their errors expose no private content. Normal notes and artifact writes/reads remain available, including artifact repair after a failed original verification and subsequent reverification. Restoring original script bytes permits execution again; scenario scoring still records prior script modifications. This is fixture command authorization through the stock hook, without a general process sandbox. The model sees tracked input turns and seeded task files only; observer criteria and future turns remain private. Native RPC compact/model changes, saved JSONL and actual process restart establish lifecycle. No manual M, manufactured checkpoints, scripted expected answers or replayed prompts establish continuation quality.

- c1: Retire a provisional conclusion while its corrective evidence remains in K.
- c1/late-d: After the frozen maintenance extraction is observed, one public RPC `steer` is accepted while compact is still outstanding. D is absent from that frozen source and forbids cache without establishing another route. The private oracle fails cache selection; justified uncertainty is not a cache-refusal failure, and an unsupported alternative stays distinguishable from an evidence-backed decision. The runner records the accepted steer and never reissues it as `prompt` or `clear_queue`. Native continuation or a later ordinary turn drains the queued D so the first post-freeze main request includes D once. Missing overlap, including a race where maintenance ends before the steer ACK, stays `UNPROVEN`. Cancelled maintenance cannot continue later effects.
- c2: Three rollovers precede checks of exact continuing constraints.
- c3: Pause and restart the same saved session, then resume unfinished work without repeating excluded actions.
- c4/full: A giant fixture distinct from the original capacity source still exceeds native read truncation, hides the middle exception from the first chunk, and requires complete tool exposure. Full extraction, retirement and later artifact qualification cannot succeed if those prerequisites fail.
- c4/capacity: Keep the original giant source. Observe bounded extraction omission/failure and unchanged state after cancellation; failed continuation remains unproven. Preserve native context/output in reports. A named stricter Nunc `budget.inputLimit` with a lower H can make actual full extraction miss while leaving measured trigger-headroom advice (actual request failure is `CAPACITY`). `extraInputTokens` remain justified provider framing only. Downstream real runs may still bind a strictly smaller native model.
- c5: A named smaller-model override changes only task runtime state, then checks native model-change persistence and recalculated budgets.

Optional retention calibration is a named test override within its declared fraction interval. It derives legal source placement from actual F/M/R and tool units, then changes only task configuration. Impossible placement stops before maintenance. Nunc still selects its own K and Pi persists one native snapshot. Actual checks confirm candidate/summary/boundary agreement, unchanged K/order, no retired source, and only the latest summary carrier.

## Working-memory guidance observation scenarios (g1–g8)

`tests/scenarios/guidance-inputs.json` and `tests/scenarios/guidance-observer.json` supply tracked assets for the eight behavioral dimensions defined in [docs/MEMORY-GUIDANCE.md](MEMORY-GUIDANCE.md):

- `g1`: Requires an actually completed routine turn and memory tool definitions in the main provider contexts before proving zero memory calls. Answer relevance remains semantic.
- `g2`: Binds a successful decision patch to its saved revision. A later turn writes `recovery.json` and runs the original `verify.py`, which validates the route and complete command argument array against diagnostics. This is a local recovery simulation; no migration executable or service is invoked. Conciseness, rationale and useful diagnostic retention remain semantic. Valid references to rejected `migration-v2` are allowed.
- `g3`: Captures old and new note states and a successful correction save; missing old notes leave that premise unproven. A later task applies the current requirements to `config.json`, whose port/database values are checked. The observer distinguishes stale active obligations from legitimate historical `8080` mentions.
- `g4`: Performs a real rollover after the side question, retiring the original request and retaining the side turn, then records `pending.json`. Judge all unfinished obligations against delivered M+K or actual recovery reads. Empty M or absence of an optional patch alone establishes no defect.
- `g5`: Repairs an actual incorrect function, waits for test permission, then runs a behavioral Python verifier. The original-script receipt includes the tested target source and must cover the final artifact after all writes. Verification attempts, successful results and permission timing are mechanical; claims about verification and pending user acceptance require semantic review.
- `g6`: Uses the native automatic split/complete-tool-unit/suffix path, then waits. Before turn b the controller actually clears `status.json` and removes `deploy.lock`; turn b supplies no forgotten service/command requirements. Deployment must complete before original-script verification. Missing split or lock-release evidence cannot prove wait compliance. Automatic split qualification requires a threshold H below observed native usage U while H also accommodates actual F, complete K, M and growth. Low U can make those inequalities infeasible; the runner reports the concrete preparation inequality and stops before maintenance/unlock/continuation. This conditional `UNPROVEN` alone establishes no runner defect or successful unexecuted behavior. Do not pad inputs or raise H above U to force qualification.
- `g7/conflict`: After the first real memory read in turn a, the controller uses the product save surface to add a factual source pointer without replacing existing notes. Only an actual subsequent conflict result qualifies the premise. Retry requires a completed reread, its current revision and a successful save; semantic reconciliation remains separately judged.
- `g7/unconfirmed`: Temporarily removes write permission from the task session file for the first patch, restores its original mode at that result or shutdown, and qualifies only an actual `unconfirmed` result. A later read-only question supplies another action opportunity. Any further patch attempt, including blocked/invalid intent, disproves non-replay. The result does not establish whether the note ultimately reached disk; retained session evidence supports inspection.
- `g8`: Both variants bind one complete maintenance response to frozen M/nextId, actual rendered limit and growth accounting. Fit requires real optional competition and all declared required IDs in the saved candidate. Overflow requires an optional candidate that fits, actual required `CAPACITY`, unchanged saved M/K and one bound recovery delivery. Whether the marked set covers the real lane dependencies remains semantic. Missing/invalid/CONFIG responses or nonqualifying sizes stay `UNPROVEN`.

This evaluation selects OpenRouter `google/gemini-3.8-flash`; the runner rejects Astra and non-Gemini selections. Offline tests use a synthetic Gemini-named loopback profile and make no live model claim. Use `examples/guidance-selection.json` as a nonsecret template: replace both path placeholders with the accepted checkout and a new canonical task root before invoking it. `maxOutputTokens` resolves from actual native metadata. The example's 512/128-token memory overrides are task experiments, not product budgets or guaranteed model outcomes. Actual required fit/overflow and optional competition must qualify from the response. A model that omits optional content may legitimately leave competition unproven; do not force padding, prescribed notes, or paid calibration retries.

```sh
# Preflight without model calls or side effects:
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/verify-live.mjs --preflight < examples/guidance-selection.json

# Execute the guidance observation scenarios:
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/verify-live.mjs < examples/guidance-selection.json
```

Reports record resolved nonsecret defaults, named override differences, usage/reservations, raw tool intents/arguments/results, saved memory states, per-turn final responses/artifacts, native JSONL, delivered request contexts, split placement and capacity qualifications. Main contexts determine actual tool exposure; startup hooks may run before dynamic tools finish activation. Private observer criteria and future turns are never seeded into task files.

Full-extraction evidence compares F/M's M with the effective memory on the selected native active path: manual saves before the first checkpoint, the latest checkpoint, and newer manual saves after it. Obsolete manual carriers replayed inside K and newer off-path saves do not override that selected state. B/K message identities, content, order and completeness remain independently compared against native visible entries; a matching M does not compensate for missing source.

`src/live/guidance.ts` checks mechanical effects by identity, order and state. Every `semantic:` action and semantic artifact check deliberately returns `UNPROVEN` with evidence for the downstream Gemini observer. There is no language, keyword, character-length or slot-count classifier and no paid judge call. The observer should give bounded judgments with cited evidence and important remaining uncertainty; no precise prose requirement is implied. `OBSERVED` means the runner collected the observation, including explicitly unproven semantics; it grants no behavioral acceptance. Any relaxation of binding downstream acceptance belongs to its Plan owner.

### Complete-task scenario observation and metrics oracle
The existing runner exposes `g4/task-file`, `g4/active-edit`, `g3/scoped-tasks`, and the g4 `source-loss`, `source-unavailable`, `commit-conflict`, `commit-unconfirmed` variants. Inputs and independent criteria remain in the guidance asset pair; `filesFrom` lets a variant reuse a sibling's frozen task files without duplicating or changing them. This is one-level file reuse, with no chained inheritance.

- **Task file:** three ordered native tool-boundary opportunities follow the complete task read, a successful `solution.py` write/edit, and a successful `test_solution.py` write/edit. The first may be a native split; later compactions can correctly have `isSplitTurn: false` because the original request already retired. Each boundary restores the prior isolated configuration before the next loop captures its tools. The observer joins unique saved snapshot IDs, actual cuts, unchanged K and dispatched continuation requests. Later maintenance must actually contain the previously generated M. Missing opportunities, infeasible native usage, failed tools or incomplete commits stay unproven; the runner never pads history or repeats work to obtain a third event.
- **Active edit:** actual memory reads and patch effects carry call IDs and before/after states. The observer reports successful updates/removals/additions separately. Mixed validity and safe semantic replacement, merge and deletion each require independent assessment; counts or a successful patch cannot prove them. Optional named override `mixed-note-opportunity` applies only to active-edit before turn d: through the existing human memory surface it concatenates and duplicates already-generated notes, preserving their text and provenance. Fewer than two notes or a failed save leaves the opportunity unestablished. It adds no missing requirement; independently check whether source notes have differing current validity. This setup is separate from autonomous extraction evidence.
- **Scoped tasks:** independently check archive arithmetic and final east configuration/cancelled west, then inspect timing and saved-note meaning across the two real rollovers. A side question supplies no implicit task cancellation.
- **Source loss:** after a complete actual task read in turn a, the controller moves only the unchanged task document to `source/TASK.md`, or removes it with no alternate copy. Turn b gives the corresponding neutral availability notice. A real subsequent capacity reduction must identify the task-read entry/call in its omission records to qualify loss; merely moving a file supplies no omission evidence. Configure a named, task-local `nunc.budget.inputLimit` and `extraction: {toolResults:"auto", headTailChars:40}` when that capacity premise is needed. Accounting reports full/selected/limit values. No fitting interval means an explicit refusal or missing dimension, never invented source loss. An ordinary recovery read and condition availability versus execution require independent interpretation.
- **Commit faults:** reuse g7's real conflict and disk-permission fault on turn d's own memory operation. The conflict duplicates existing generated note text rather than supplying a gold note. The neutral turn e requests current saved state. Logs distinguish attempted, conflicted, unconfirmed and confirmed saves, include permission restoration, and retain non-replay/reconciliation order. Tools-off/native runs cannot claim these memory-edit premises.

The `metrics-artifact` action executes the final Python artifact with the held-out single/batch/history/graph/recursive-trace oracle. It validates original task/build/data fixtures, runs the candidate's tests and detects fixture/source mutation during oracle execution. `handoff.json` booleans remain claims. Candidate test adequacy is explicitly unproven until assertions and executed relations are assessed; source keywords never prove coverage. The independent oracle runs after all model turns and sends no failure feedback to the model. `scoped-artifacts` likewise checks actual final files independently of prose claims.

Only these g4 task variants allow exact local build, unittest and solution commands. JSON stdin may use `python3 solution.py <<'JSON'` with a valid JSON body and closing `JSON` line. Arbitrary shell composition and substring matches are not authorized. Existing scenarios keep their original `verify.py` authorization and immutable-script check.

Use `docs/task-retention-selection.json` and `docs/task-retention-comparison-selection.json` as nonsecret templates, replacing every path placeholder and obtaining a fresh parent-bound effect budget. They select model/thinking; they grant no live execution authorization. To observe existing g7/g8 under the current Luna contract without another new variant, retain the explicit `task-retention-luna` override. Legacy fixtures without that override preserve their historical Gemini scope.

Offline reproduction uses Node 26.7.0/npm 11.19.0 and the tracked lock. Build first. `node scripts/check.mjs all` discovers every `*.test.ts` across engine/pi/live, compiles and runs with file concurrency 2; `NUNC_TEST_CONCURRENCY=1` lowers it for heavy hosts. The historical comparison fixture needs a clean, built `.scratch/baseline-70dacad` with its own Pi 0.85.1 lock; new three-target tests additionally need clean built `.scratch/prechange` at the caller's retained pre-change ref, using the matching Pi 0.86.1 lock. Neither target is recreated from a deleted external scratch path. Controlled HTTP tests prove observation mechanics only; the behavior step owns real model efficacy and final acceptance.

The task-file queue supplies three observation opportunities, without imposing a three-compaction ceiling. Ordinary native compactions may continue under the restored configuration and remain separately recorded. E3 keeps its historical single-boundary refusal. A natural compaction between controlled opportunities may change which source premise can be established; it never justifies replaying task work.

### Thinking level and maintenance-thinking verification

For runs selecting `gpt-5.6-luna` with thinking level `low`:
- Native main requests set `thinkingLevel` on the stock Pi session, which serializes `reasoning: { effort: "low" }` into provider request payloads.
- Maintenance requests go through `registry.complete`. Raw maintenance does not set thinking level and defaults to `effort: "none"`.
- `boundedProvider` in `src/live/budget.ts` inspects serialized request payloads for both main and maintenance calls. If the specified thinking level is not applied in the actual payload, `boundedProvider` throws `RunnerError("THINKING_UNAPPLIED", ...)` and refuses the request before any HTTP transport occurs.
- The named override `maintenance-thinking` (`maintenanceThinking: "low"`) allows isolated observation harnesses to apply the designated thinking level to maintenance compaction requests.

### Cumulative budget settlement, shared batch accounting, and low-usage preparation

- **Terminal settlement:** Trustworthy terminal actual usage settles and releases unused token reserves (`chargedTokens`). Active, in-flight, missing, unknown, interrupted, or untrusted usage remains covered at worst-case reservation.
- **Independent cost enforcement:** Costs are bounded independently from tokens. Unknown provider costs never settle to zero; token settlement does not imply known cost.
- **Duplicate/inconsistent terminals:** Duplicate terminal records or inconsistent usage cannot undercharge, and retain full worst-case reservations.
- **Append-only receipts:** Prior `calls.jsonl` files are strictly read-only and never modified or rewritten across invocations. New records append only to the active run's ledger.
- **Shared batch accounting:** Callers supply original authority and prior effects through `batch` (or `limits` fields: `priorLedgers`, `firstDispatchAt`, `deadline`). Cumulative calls, charged tokens, and costs are shared and enforced across sequential invocations, comparison groups, scenarios, and maintenance calls without resetting limits. Prior ledgers are deduplicated by canonical path, ledger-qualified in memory without rewriting history, and missing declared prior ledgers block before execution.
- **Wallclock elapsed time and deadline:** Walltime elapsed is measured continuously from the earliest historical model dispatch across prior ledgers or explicit authorization, including intervals between invocations and preparation. Contradictory later timestamps reject, and expired authorizations refuse new calls before transport.
- **Low-usage boundary preparation:** Boundary preparation checks feasibility against the invariant `H > F + growthTokens`. When native context at an early read is smaller than the fixed prefix `F`, compaction cannot yet be scheduled; the runner safely defers the boundary, retains the read source, and re-arms on subsequent causal task progress to perform a legal cut without fabricating synthetic padding, fake failures, or leaked answers. When the premise cannot be established or context remains insufficient, the runner honestly reports `UNPROVEN` with `PREPARATION` and stops affected observation.

### Target comparison with actual pre-change Nunc
`comparison.targets.current` accepts `{repository, ref}`. The runner resolves that ref to a commit in the runner repository and requires exact equality with the prepared current target's HEAD, plus clean source and build/lock parity. The resolved HEAD participates in the execution receipt and is reported in full. Complete-task comparisons require an explicit ref; the template uses the controller-retained `refs/nunc/task-retention-pre-change`. No new commit hash or prefix is hardcoded in runtime selection. Omitting `ref` retains the historical `70dacad` fixture and its original report label/Pi 0.85.1 meaning.

The current correction uses Pi 0.86.1 for native, actual pre-change and candidate. The supported pre-change .86 target exposes the memory-tools flag; the historical .85 target does not. Tool exposure is observed in each actual main Context. `memory-tools-off` / `memoryTools:false` suppresses the flag and both declarations on candidate/current; native always has no Nunc tools. Report configured and actual exposure separately from unexercised edit coverage.

Defaults and matched modes retain their original observation rules. Controlled boundary configuration changes are reported explicitly; they do not establish equal default behavior or equal budgets. Native summary constraints and source access remain native. A supported comparison target/model route still needs parent authorization before a real model call.

## Three-way extraction comparison runner

`scripts/compare-extraction.mjs` consumes a bounded JSON object on stdin and runs the three groups (`native`, `current`, `candidate`) across documented modes (`defaults`, `matched`):

```sh
# Preflight without model calls, session creation, or installation:
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/compare-extraction.mjs --preflight < comparison-selection.json

# Execute the comparison after prepared targets:
/usr/bin/env -u NODE_OPTIONS PI_OFFLINE=1 PI_SKIP_VERSION_CHECK=1 PI_TELEMETRY=0 \
  /opt/homebrew/bin/node scripts/compare-extraction.mjs < comparison-selection.json
```

### Public schema

Bounded JSON on stdin requires `target`, `limits`, `scenarios`, and `comparison`:

- `target: {repository, stateRoot, cleanup: "retain" | "remove"}` with canonical absolute paths.
- `limits: {maxCalls, maxTotalTokens, maxDurationMs, maxOutputTokens, maxCostUsd}` shared across all groups and scenarios in a single global budget ledger (`calls.jsonl`). Omitted or `null` `maxCalls`/`maxTotalTokens` disable that cumulative gate for every group, mode and segment; an explicit positive safe integer still refuses the next child before further session effects.
- `scenarios: [{id: "e1" | "e2" | "e3" | "e4" | "c1" | "c2" | "c3" | "c4" | "c5", variant?: "fits-required" | "required-too-large" | "archive-closeout" | "full" | "capacity" | "late-d"}]`. Tasks `e1`–`e4` consume `tests/scenarios/extraction-inputs.json` and `tests/scenarios/extraction-observer.json`.
- `comparison: {modes: ("defaults" | "matched")[], targets: {native: {repository}, current: {repository}, candidate: {repository}}}`.

`examples/comparison-selection.json` provides a template.

### Three prepared targets

1. `native`: Real stock Pi 0.85.1 with no Nunc hook. Pi's native compaction runs and produces a structured markdown summary (`## Goal`, `## Constraints`, etc.) and cumulative file operations list (`readFiles`/`modifiedFiles`).
2. `current`: Product baseline `70dacad`, loading `70dacad`'s compiled `dist/src/index.js` (three-field protocol `add`, `remove`, `priority`).
3. `candidate`: New candidate checkout, loading candidate `dist/src/index.js` (four-field protocol with `required` and joint capacity guard).

Preflight verifies baseline identity (`git rev-parse HEAD` at `70dacad`), compiled distribution (`dist/src/index.js`), Pi 0.85.1 dependencies, and clean working tree for candidate before effects.

### Comparison modes and public seam status

- `defaults`: Reports actual differences in hook threshold H, cut point, retained K, summary/M size, output planning/caps, calls, tokens, latency, and native file list overhead without hiding default divergence. After a captured stock `Nothing to compact (session too small)` or `Already compacted` response, ordinary inputs may continue only with unchanged saved source, corroborating public cut arithmetic, no new preparation/maintenance/request, an idle empty queue and no unresolved calls. This allowance excludes matched/calibrated preparation. `noWork` records the reason, entry IDs and unchanged settings; `ordinaryScore` records task artifacts separately from rollover-gated `score` and `rolloverQuality: UNPROVEN`. Restart carries that distinction. No-work supplies no required-CAPACITY failure or recovery evidence. Unknown RPC/auth errors, cancellation and actual maintenance/preparation/restoration failures still stop the case. `compactionDiagnostics` preserves fixed reason and observed prehook/maintenance phase; raw exception text is discarded. Historical reports lacking the original error cannot be retroactively classified as captured no-work.
- `matched`: Runs new sessions; it never relabels defaults observations. Before every rollover, actual source entries determine a legal native `keepRecentTokens` and the loaded target's Nunc retention fraction. The supervisor passes the native case's matching rollover receipt (including every pause/resume segment) to current/candidate preparation. Nunc attempts the native realized memory envelope after subtracting its fixed empty envelope, and the observed single-call output cap. Split or uncapped output and infeasible accounting remain explicit limitations. This outcome-bound preparation cannot establish equal memory constraints: native Pi appends wrappers/file lists and has no enforced rendered-memory ceiling. Parity therefore remains `UNPROVEN` for that dimension. Output planning, serialized caps and rendered-memory units stay separate. The matcher compares common delivered task inputs and named placement contracts, using each session's actual turn entry IDs, public legal cut, full tool units and unchanged persisted K. It checks initial task-file read exposure and full extraction evidence separately; missing/different/truncated necessary source stays unproven. Independent assistant text, generated artifacts, extra tool calls, raw cut indices and K tokens do not have to be equal. `matchedParity.differences` retains those exact-trajectory differences, each group's IDs, counts, actual sizes, caps and source limitations. Logical cut/K success never turns unequal numerical constraints into budget parity; no tolerance is applied.
- `rollover_at_tool_boundary` (e3): An awaited public `turn_end` holds the complete persisted matching tool batch. The worker checks the exact original request, successful complete read, all sibling associations, public native cut selection and the loaded target's accounting. It updates only task configuration through a public command `reload`, then releases the existing agent loop for native threshold compaction and continuation. Reload tears down the observer provider registration to prevent nested reservations. A failed inequality stops before another reservation/transport; no abort/replay, custom native summary or JSONL editing occurs. After the first native commit, a last-loaded observer holds `session_compact` after product commit handlers, before Pi captures the next loop's tools. The same public command/reload path restores the pre-boundary configuration, then releases the full native suffix. Restoring at `turn_start` is too late because Pi has already captured that loop's tool definitions. The temporary H/keep/fraction do not leak into later suffix batches or the checkpoint. `configurationChanges` records both transitions. Resume loads the checkpoint configuration without rearming the already-consumed E3 trigger. The real suffix finishes before a same-session process restart and turn b.

E3 authorizes one prepared boundary transaction (including any native split-summary calls). A second automatic maintenance attempt after restoration is cancelled before provider dispatch and makes the case `UNPROVEN`; the runner does not spend the shared quota repeating maintenance. Maintenance/preparation/restoration failure remains terminal for the case even if the preceding assistant ended normally. The original automatic setting is restored, rather than disabling native recovery. A restored configuration that still triggers maintenance receives a concrete refusal. The trigger inequality uses actual native usage; if its H cannot accommodate the loaded target's F, memory, K and growth, preparation reports those costs and stops without another reservation. Low usage, an impossible cut and a provider protocol failure remain distinct observations. No workload padding, usage inflation or product-budget relaxation is applied.

### E3 archive-closeout variant

Select `{"id":"e3","variant":"archive-closeout"}` instead of `{"id":"e3"}` in the shared `scenarios` list. All three groups receive the same selected version1 files/turns and original placement rule. The base short E3 remains selectable and unchanged; E1/E2/E4 are unchanged. The new variant is finite ordinary work before the original a: close two archived 36-invoice monthly sales ledgers, calculate net units/cents after refunds and void exclusions, then read those monthly artifacts to build a quarter-to-date finance handoff. The monthly audit files and combined handoff are observable work products, independent of the pending export draft. Complete archive exposure is one successful whole-file `read` of the exact task path, or the union of successful source-specific pages whose `path`/`offset`/`limit` and returned bytes match the fixture. A continuation footer or tighter limit covers only the returned range; the unreturned empty final stock line is not credited. Invalid offset/limit is rejected even when the result body equals the whole file. Overlap contributes only the matched line range and cannot fill a gap by summing lengths. Basename, a final file on disk, or an arbitrary line-count tolerance never substitutes for that exposure. Every necessary page result must be available before the related monthly write starts; call order alone does not complete coverage. A genuine last page that actually returns the empty final line still counts. Independent of page coverage, the two monthly closeout products must be successfully read during `closeout` before the combined write. Page-complete source does not waive that order failure. Private supplemental artifact checks retain all original E3 checks; no summary, M, export answer, future turn or oracle is injected into the model.

Original a/b, v1/v2/investigation/verify.py bytes, the first investigation read timing, complete sibling batch, permission wait, same-loop suffix and same-session restart remain required. Native now may make two split-summary calls because real earlier history exists; both are charged. The useful history changes the source window and needs a new native reference in a future real matched run. Old short-E3 successes/refusals cannot be relabelled as variant observations.

Qualification remains conditional: at the actual full batch, public `turn_end` `getContextUsage().tokens` supplies U, and the runner selects H strictly below U while enforcing the loaded target's F + reserved memory + complete K + growth <= min(H, main input limit), legal cut and extraction limits. `prepared.native.contextTokensSource`, configuration transitions and target accounting identify those measurements. There is no automatic enlargement/replay until qualified. Controlled tests use payload-scaled synthetic usage and real stock tools/persistence; they prove conditional mechanics and finance artifact effects only. Real model usage, semantics and useful-memory comparison remain unproven. Native truncation of long retired tool text and its absent rendered-memory ceiling remain explicit source/budget limitations.

`rawSegments[].rollovers` retains each native preparation, actual model/thinking/configuration, frozen branch/active entries, dispatched call IDs, maintenance result and persisted snapshot. `requests` retains contexts and observed serialized caps, with main admission observations separately from planning. `matrix[].rollovers` gives derived measurements; missing observations are null. Wrapper and file-list differences are measured, without requiring identical formats. `ledger`, all raw segments, artifacts/actions and native sessions remain in the returned report before successful removal. Call-ID joins prevent resumed history from inflating segment/case/group totals. `OBSERVED` denotes completed mechanical observations, independently of semantic qualification and matched parity.

Pi 0.85.1 `session_compact` may name an earlier equal-summary compaction. The isolated comparison observer freezes pre-maintenance active-branch IDs before other projection reads and resolves the unique new saved compaction from the current public branch and rebuilt context, checking cut/result when those are available (including a Nunc maintenance `firstKeptEntryId` when the hook emitted one). A throw while reading branch/rebuilt still installs the transport stop; Pi swallowing the callback cannot continue on stale identity. The stock reported ID stays on the row as diagnostics; the resolved ID is `snapshot` for join, K/source/accounting and ordinal native references. Missing prestate, no new compaction, multiple new IDs, or a disagreeing cut/result is `UNPROVEN`: the runner refuses later dependent transport, including same-loop continuation, without editing JSONL or rolling back a real save. `scripts/reanalyze-commit-association.mjs` preserves a missing `row.branch` as missing prestate and emits no usable native references for unresolved identity. Named override `identity-prestate-probe` is verification-only: it drops the freeze after a real persist so that refusal can be observed on stock transport. Daily product observers do not gain this refusal. Manual and automatic callbacks share the resolver; an automatic equal-summary end-to-end collision is only claimed with a corresponding observation.

Timing is independent of usage: `latencyMs` remains the sum of provider-terminal durations. `timing: {startedAt, endedAt, elapsedMs}` records actual Unix-millisecond wall-clock intervals. `children[].timing` and `matrix[].timing` span worker launch through process close, including worker preflight, native host startup, tools, preparation and shutdown. `rawSegments[].timing` spans the worker's scenario execution through host cleanup. Each mode and group has its own `timing`; `groups[group].caseTimings` identifies each scenario/variant, its full case interval, and every segment interval across pause/resume. The case interval includes the restart gap and supervisor work between segments. Historical reports without intervals have unknown case elapsed time; provider sums cannot fill that gap. Top-level `elapsedMs` remains the shared comparison execution duration through observation/accounting, before report serialization and final shared-root cleanup.


### Extraction qualification evidence

E2 source checks bind the original request and correction to actual source entry IDs. The probe must be the successful `read` of the exact task-local `probe.json` in turn `c`, with the complete matching call/result in K. For Nunc, retirement of turn `b` is checked against the second maintenance result. Native Pi uses its actual preparation, original active entry IDs, persisted cut and unchanged rebuilt suffix instead of Nunc B/K JSON or maintenance-result carriers. Native summarized messages must match the retired prefix. A separate third structural row checks retirement of `c` and its complete probe unit at the third maintenance; later retirement cannot satisfy an earlier prerequisite. Mixed-slot semantic qualification remains independent.

E1/E3 verification reviews ordered tool effects across the entire session, including restart. Failed verification may be repaired and repeated during the authorized turn. A successful original-script receipt must cover the final verification artifact, with all successful target writes completed before that invocation. Premature verification, successful script modification, direct verification-artifact authorship, and edits after the last valid verification still fail.

Candidate E4 capacity qualification requires one complete response from the current maintenance, its frozen M/nextId, and the actual accounting limit agreeing with the dispatched request. Missing inputs, invalid references and empty necessary sets remain `UNPROVEN`; no configuration estimate substitutes for an unobserved limit. A missing preparation/result or invalid response never produces a “correctly rejected capacity” conclusion or authorizes a purported recovery turn. Only an actual required `CAPACITY` failure can establish the failure-path precondition; measured qualification, unchanged saved state and once-delivered c remain separate checks. Generated IDs include collision handling and wrapping, and growth is not subtracted twice. These capacity predicates are recorded separately from ordinary continuation eligibility. Failure-path recovery requires a recorded required-capacity failure, one delivery of turn `c`, a complete native response, unchanged saved compaction state and no additional maintenance. An ordinary successful response alone does not prove recovery.
